import React from 'react';

const About: React.FC = () => {
  return (
    <div style={{ color: '#ffffff' }}>
      <h2>About QueryDesk AI</h2>
      <p>QueryDesk AI is a semantic search platform that enables employees to retrieve relevant internal documents using natural language queries.</p>
      <p>It supports file and text upload, document grouping by department, and semantic ranking for better discovery.</p>
    </div>
  );
};

export default About;